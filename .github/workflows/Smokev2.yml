on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Local Action Call
        id: local-action
        uses: Smoke-TestsV2/tests/smoke-test1.yaml@main
 
